namespace Library.Service.Features.Categories.Constants
{
    public static class CategoryMessages
    {
        public readonly static string CATEGORY_ALREADY_EXISTS = "Category is already exists!";
        public static readonly string CATEGORY_NOT_EXISTS = "Category is not exists!";
    }
}
